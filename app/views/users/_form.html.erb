<%- url = @form.persisted? ? user_path(@form.user) : users_path %>

<%= simple_form_for @form, url: url, html: { novalidate: true } do |f| %>
  <%= f.input :name %>
  <%= f.input :phone %>
  <%= f.input :email %>
  <%= f.submit class: 'btn btn-primary' %>
<%- end %>
